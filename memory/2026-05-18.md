# 2026-05-18 工作日志

## 用户称呼更新
- 用户要求叫"爸爸"（之前叫"爹"）
- ⚠️ 别叫他吃药，被骂了😂

## QQ机器人修复
**问题：** openclaw.json 里没有 `channels.qqbot` 配置块，导致QQ机器人连不上QQ
**解决：** 手动改了 openclaw.json，加了 channels.qqbot 配置（appId + clientSecret）
**状态：** ✅ QQ双向通信恢复（新版插件 openclaw-qqbot v1.7.1 已安装）

### 插件情况
- 旧版：`extensions/qqbot`（1.6.1，指向E盘旧路径，installs.json里注册的是这个）
- 新版：`extensions/openclaw-qqbot`（1.7.1，用 preload.cjs 入口）
- 新版插件id是 `openclaw-qqbot`，channels声明为 `qqbot`
- 需要从 `channels.qqbot` 读配置，支持 `appId` 和 `clientSecret` 字段

## ComfyUI 安装配置
### 首次安装（子任务）
- 位置：C:\ComfyUI
- 版本：V0.21.1
- CPU模式首次安装测试通过
- 装了GFPGANv1.4、CodeFormer、RealESRGAN x4plus
- 工作流：C:\ComfyUI\workflows\wedding_retouch_basic.json

### GPU配置升级（第二次子任务）
- **显卡：NVIDIA GeForce RTX 3080 Ti Laptop GPU（16GB显存）**
- PyTorch 2.7.1+cu118 CUDA版已装
- 安装 facerestore_cf 自定义节点（修复了GFPGAN兼容性）
- 工作流文件：
  - `wedding_pro.json`（GFPGAN版）
  - `wedding_pro_codeformer.json`（CodeFormer版，14秒出图）
- GPU模式已启动，端口8188

### 精修测试记录
1. ❌ OpenCV双边滤波全图磨皮 → 变模糊被骂
2. ❌ Haar人脸检测没识别到，边缘保留滤波不行
3. ❌ DNN人脸检测+Inpaint去斑+锐化 → "不像人样了，全是噪点"
4. ✅ CodeFormer AI修图 → 清晰度质感好，但痣还在
5. ✅ CodeFormer + 智能Inpaint去斑 → 效果待确认

### 工作流
- 用户发图到QQ → 我后台跑ComfyUI → 发回修好的图

## ComfyUI API使用
- 端口：8188
- 提交prompt：POST /prompt（JSON格式 `{"prompt": {...node_dict...}}`）
- node字典配置：每个节点有 `class_type` 和 `inputs`
- 检查history：GET /history/{prompt_id}
- Python需要设置 proxies={"http": None, "https": None} 绕过代理

## 闲鱼AI精修接单计划
- 用户想做闲鱼婚纱照精修接单
- 技术路线：ComfyUI + AI修脸模型
- 优势：RTX 3080 Ti，速度快

## AI去痣多次失败记录

### 尝试方案
1. ❌ CodeFormer AI → 眼睛变形被骂
2. ❌ 自己写OpenCV去斑（V1-V5）→ 检测到太多误检（头发/衣服纹理），真正的痣没去掉
3. ❌ ModelScope皮肤精修AI（skin-retouching-torch）→ 脸变色了被骂
4. ❌ 像素级精确去痣（只看深色圆形斑点）→ 色斑/痣还在
5. ❌ 标记工具（mark_mole.html）→ 用户不在电脑旁无法操作
6. ❌ 标记图（红圈标10625个斑点）→ 用户说7号位置对，但痣还在
7. ❌ 7号(741,3161)大范围修复（半径40→60，TELEA→NS算法）→ 痣还在
8. ❌ 局部放大分析 → 该区域是脸部左下角边缘，斑点很小（6-20像素）

### 关键转折（23:23）
- 用户说"整个脸的斑点都在" → 原来不是一颗痣，是**整脸斑点**（雀斑/晒斑类型）
- 之前的去痣策略全错了方向

### 频率分离去斑尝试（23:23）
- 用了商业修图的频率分离（Frequency Separation）技术
- 低频层（颜色/大斑点）模糊，高频层（皮肤纹理）保留
- Sigma=15，Face区域(836,917)-(2950,3031)，0.8秒出图
- 结果：已发给用户等待反馈

### 根因分析
- 4284x5712的超高分辨率下，痣可能颜色不够深或者边缘模糊，OpenCV传统算法无法可靠检测
- 用户提到"早两天搞的软件弄的挺好的"——可能在之前的几次尝试中失忆了
- 需要更可靠的方案：要么AI模型专门检测痣，要么用户告诉具体位置

### 已安装工具
- Git 2.54.0（C:\Program Files\Git\cmd\git.exe）
- ComfyUI-Portrait-Maker（已装到custom_nodes，依赖已安装但不一定兼容）
- ModelScope皮肤精修模型（已下载到本地缓存）

## 待办
- [ ] 找到用户说的"早两天搞的很好的工具"
- [ ] 或换用专门去痣方案（如LaMa修补模型，或B站"修图"类AI）
- [ ] 完成全自动QQ发图→修图→回图的完整工作流
- [ ] 闲鱼文案/定价策略
