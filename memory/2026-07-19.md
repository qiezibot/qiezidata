# 2026-07-19 日志

## U-Claw 重启 & QQ 机器人修复（21:14-21:48）

### 背景
爹启动了备份版 U-Claw v1.1，Gateway 重启后 QQ 机器人两个插件（openclaw-qqbot、qqbot_backup）都因缺少 `silk-wasm` 模块加载失败。

### 修复过程
1. **silk-wasm 安装** — 官方版 openclaw-qqbot 已装好（backup 版装不上，但不需要了）
2. **config 被覆盖问题** — U-Claw 控制台启动时覆盖了 openclaw.json，导致 plugins.allow 和 channels.qqbot 配置丢失。多次写入 config 才保住
3. **plugins.allow 配置** — 设为只允许 openclaw-qqbot，备份版 deny 掉
4. **最终状态** — QQ 机器人 WebSocket 连接成功，appId=1904006743，Gateway ready

### 语音管道重建
edge-tts + ffmpeg + pysilk 方案因以下问题需要重建：
- Python 被清理时删了 → 重装 Python 3.11.9（`C:\Users\lfy20\AppData\Local\Programs\Python\Python311\python.exe`）
- ffmpeg 也被删了 → 从 gyan.dev 重下 ffmpeg 8.1.2 到 `C:\temp\ffmpeg.exe`
- edge-tts 连微软服务器时好时坏 → 加了 SelectorEventLoopPolicy 修复 Windows asyncio bug
- pysilk 是空壳包（只有 dist-info）→ 改用 Node silk-wasm 编码
- gTTS 连不上谷歌（墙）→ 放弃

### ✅ 最终方案：本地 TTS（pyttsx3）+ Node silk-wasm
- **TTS**: pyttsx3（Windows SAPI5，本地不联网）
- **格式转换**: ffmpeg 转 WAV 24000Hz 16bit mono
- **Silk编码**: Node silk-wasm（openclaw-qqbot 扩展自带）
- **脚本路径**: `C:\temp\tts\_voice_local.py`
- **输出**: direct 发 <qqvoice> 标签

### 语音模式约定
- 爹要求：以后不管他发文字还是发语音，我只用纯语音回复（不带文字）
- 算力策略：尽量用本地算力，算力不够再调 API

### 技术点
- Python 3.11.9 路径：`C:\Users\lfy20\AppData\Local\Programs\Python\Python311\python.exe`
- pip 安装 pyttsx3 依赖 pywin32 + comtypes
- Windows 需要先运行 `python Scripts/pywin32_postinstall.py -install`（但本次直接可用）
